# ドキュメント間 不整合・矛盾レポート

> 作成日: 2026-03-01
> **最終更新: 2026-03-06（実コード整合性チェック追加）**
> 対象: Doc_dashboard/ 内の全ドキュメント（38ファイル） + 実装コード

---

## 📊 調査結果サマリー（2026-03-06 更新）

| 分類 | 件数 | 状態 |
|------|------|------|
| **🔴 重大（実装済みだがDoc未記載）** | 4件 | アンケート・スタッフ・ログ機能がDoc_dashboardに未反映 |
| **🔴 重大（ロジック矛盾）** | 6件 | 既存の矛盾（変更なし） |
| **🟡 中程度の不整合** | 10件 | 既存8件 + 新規2件（ナビゲーション・型定義） |
| **🟢 軽微な不整合** | 4件 | 既存のまま |

---

## 🔴 重大（実装済みだがDoc_dashboardに未記載）

### 実A. アンケート機能のドキュメントが整理不完全

**問題点:**
- Doc_dashboard に `40_アンケート機能仕様検討.md` など4ファイルがあるが、**完全な統合版がない**
- 旧 Doc フォルダの内容（86-89）と重複・分散している

**実装状況:**
- ✅ 完全実装済み（`app/admin/surveys/*`, `app/api/surveys/*`, `supabase/017-018`）
- ✅ ナビゲーションメニューに「アンケート」リンクあり

**推奨:**
- 40-43 を統合して1つの `40_アンケート機能仕様書.md` にまとめる
- または Doc/86-89 をマージして Doc_dashboard に配置

---

### 実B. データベーススキーマに最新テーブルが未記載

**問題点:**
- `05_Database_Schema.md` が古く、以下のテーブルが未記載:
  - `surveys`, `survey_answers`, `survey_targets` (アンケート)
  - `staff` (スタッフアカウント)
  - `activity_logs` (スタッフ操作ログ)
  - `event_logs` (ユーザー行動ログ)
  - `care_messages` (詳細定義なし)
  - `broadcast_logs` (詳細定義なし)

**マイグレーションファイルとの不整合:**
- Doc記載: 001-012 まで
- 実際のファイル: 001-018 まで存在

**推奨:**
- `05_Database_Schema.md` を全面的に更新
- マイグレーション実行順序を 013-018 まで追記

---

### 実C. ナビゲーション構造の記載が実装と異なる

**問題点:**
- `01_ファイル構成.md` に記載されているメニュー構造が古い

**記載内容:**
```
/admin, /admin/analysis, /admin/broadcast, /admin/reward-exchanges
```

**実際のナビゲーション (`lib/admin-nav.ts`):**
```typescript
mainNav: 患者一覧, 分析, アンケート, 一斉配信, 特典交換
toolNav: テストQR
logNav: スタッフ操作ログ, ユーザログ
accountNav: パスワード変更
```

**推奨:**
- `01_ファイル構成.md` のナビゲーション構造を実装に合わせて更新

---

### 実D. 型定義にアンケート関連型がない

**問題点:**
- `lib/types.ts` にアンケート関連の型定義が存在しない
- 実装コードでは Survey, SurveyTarget などの型を使用中

**推奨:**
- `lib/types.ts` にアンケート関連型を追加するか、別ファイルに分離

---

## 🔴 重大（ロジック矛盾・バグの原因になりうる）

### 1. reward_exchanges の `completed_at` / `completed_by` カラム問題

**矛盾内容**: 07_特典交換履歴機能仕様書のTypeScript型定義に `completed_at`, `completed_by` が含まれているが、同じドキュメント内で「実際には存在しません」と明記。05_Database_Schemaのテーブル定義にもこれらのカラムはない。

| ドキュメント | 記載内容 |
|-------------|---------|
| 07_特典交換履歴（型定義） | `completed_at: string \| null` / `completed_by: string \| null` を定義 |
| 07_特典交換履歴（注意書き） | 「`completed_at`、`completed_by` カラムは実際には存在しません」 |
| 05_Database_Schema | reward_exchanges に `completed_at`, `completed_by` なし |

**推奨**: 07の型定義から `completed_at`, `completed_by` を削除。ステータス更新処理の記述も修正。

---

### 2. stamp_history トリガーの計算方式不統一（MAX vs SUM）

**矛盾内容**: INSERT時は `MAX(stamp_number)` で計算、DELETE時は `SUM(amount)` で計算。同じスタンプ数を求める処理が異なる方式。

| ドキュメント | 記載内容 |
|-------------|---------|
| 016_stamp_history_DELETEポリシー検討 | INSERT時: `MAX(stamp_number)` / DELETE時: `SUM(amount)` |
| 05_Database_Schema | スタンプ数 = `MAX(stamp_number)` |

**推奨**: 016で提案されている案B（MAX方式で統一）を採用・確定。

---

### 3. 一斉配信 preview API の HTTPメソッド矛盾

**矛盾内容**: 同じエンドポイントが異なるHTTPメソッドで定義されている。

| ドキュメント | 記載内容 |
|-------------|---------|
| 06_一斉配信機能仕様書（詳細版） | `GET /api/broadcast/preview` + Request Body |
| 06_一斉配信機能仕様書_シンプル版 | `POST /api/broadcast/preview` |

**推奨**: POST が正しい（GET でRequest Bodyを持つのはHTTP仕様上不適切）。シンプル版に統一。

---

### 4. スタンプ `-1` ボタンと積み上げ式の矛盾

**矛盾内容**: スタンプは「積み上げ式（減らさない）」が基本方針だが、実装コードに `-1` ボタンが存在。

| ドキュメント | 記載内容 |
|-------------|---------|
| 08_重要_スタンプ仕様_積み上げ式 | 「絶対にやってはいけない実装」として減算を禁止 |
| Doc_実コード整合性チェック結果 | `patients-table.tsx` に `updateStampDelta(p.id, -1)` が存在 |

**推奨**: 緊急修正用の `-1` を許容するなら、10_重要_スタンプ仕様に例外ケースとして明記。

---

### 5. イベントログのテーブル名分裂（activity_logs vs event_logs）

**矛盾内容**: スタッフ操作ログとユーザ操作ログで異なるテーブル名・スキーマが使われており、05_Database_Schemaにどちらも記載がない。

| ドキュメント | テーブル名 | IDカラム型 | メタデータカラム |
|-------------|----------|----------|---------------|
| 23_イベントログ設計（スタッフ） | `activity_logs` | UUID (staff_id) | `details` (JSONB) |
| 83_イベントログ設計（ユーザ） | `event_logs` | TEXT (user_id) | `metadata` (JSONB) |
| 84_イベントログ仕様（開発者向け） | `event_logs` | TEXT (user_id) | `metadata` (JSONB) |
| 05_Database_Schema | **記載なし** | — | — |

**推奨**: 05_Database_Schema に両テーブルの定義を追記。統合するか分離するかを決定。

---

### 6. stamp_method に 'survey_reward' が未登録

**矛盾内容**: アンケート回答報酬でスタンプ付与する際の `stamp_method: 'survey_reward'` が、05_Database_Schemaの定義に含まれていない。

| ドキュメント | 記載内容 |
|-------------|---------|
| 87_アンケート機能_懸念点対応方針 | `stamp_method: 'survey_reward'` を使用 |
| 05_Database_Schema | stamp_method は 'qr_scan', 'manual_admin', 'import' のみ |

**推奨**: 05_Database_Schema の stamp_method 値一覧に 'survey_reward' を追加。

---

## 🟡 注意（混乱を招く不整合）

### 7. イベントログ画面の実装状態が矛盾

| ドキュメント | 記載内容 |
|-------------|---------|
| 23_イベントログ設計（スタッフ） | 「一覧画面の実装は必須としない。DB上で確認できれば十分」 |
| 84_イベントログ仕様（開発者向け） | 「✅ 実装済み」「管理ダッシュボード側で実装してほしいこと」 |

**推奨**: 実際の実装状態を確認し、記載を統一。

---

### 8. 一斉配信の実装方針が2バージョン併存

| ドキュメント | 記載内容 |
|-------------|---------|
| 06_一斉配信機能仕様書（詳細版） | 3ステップウィザード形式 |
| 06_一斉配信機能仕様書_シンプル版 | 1画面完結（「❌ 3ステップウィザードUI」と明記） |

**推奨**: 詳細版を archive に移動し、シンプル版を正規版（12番）とする。（整理案v2で対応予定）

---

### 9. broadcast_logs テーブルの `created_at` カラム有無

| ドキュメント | 記載内容 |
|-------------|---------|
| 06_一斉配信機能仕様書（詳細版） | `sent_at` + `created_at` の2つ |
| 06_一斉配信機能仕様書_シンプル版 | `sent_at` のみ（`created_at` なし） |

**推奨**: シンプル版を正規版とした上で、`created_at` の要否を判断。

---

### 10. アンケート機能のフェーズ定義が文書間で不一致

| ドキュメント | Phase 4 の範囲 |
|-------------|---------------|
| 86_アンケート機能仕様検討 | 結果集計・グラフ表示（Phase 6に記載） |
| 88_アンケート機能_開発計画 | 結果集計・配信設定 |
| 89_アンケート機能_Phase4_集計 | 集計結果・一覧確認・配信設定 |

**推奨**: 88（開発計画）をマスターとし、他のフェーズ記述を合わせる。

---

### 11. `display_name` vs `line_display_name` カラム名混用

| ドキュメント | 使用カラム名 |
|-------------|------------|
| 05_Database_Schema | `display_name` ✅ |
| 86_アンケート機能仕様検討 | `p.display_name` ✅ |
| 87_アンケート機能_懸念点対応方針 | `line_display_name` を指摘→修正 |

**推奨**: 正しくは `display_name`。87内の修正提案が反映済みか確認。

---

### 12. スタッフ操作ログの保持期間が未定義

| ドキュメント | 保持期間 |
|-------------|---------|
| 23_イベントログ設計（スタッフ） | 「無期限でもよい。1年経過で検討」 |
| 83/84_イベントログ設計（ユーザ） | 400日（明確に定義） |

**推奨**: 監査要件を考慮し、スタッフ操作ログの保持期間を明確に定義。

---

### 13. 特典交換の削除機能：API定義なし

| ドキュメント | 記載内容 |
|-------------|---------|
| 07_特典交換履歴機能仕様書 | UI に「削除」ボタンあり（Phase 1 ✅） |
| 同上 | API仕様セクションに DELETE エンドポイントの定義なし |
| 100_機能一覧 | 「履歴削除：誤操作時に履歴を完全削除」と記載 |

**推奨**: DELETE APIエンドポイントを明示的に定義。

---

### 14. NULL staff_id の扱い（過渡期対応の明確化）

| ドキュメント | 記載内容 |
|-------------|---------|
| 23_イベントログ設計 | `staff_id` NULL許容（「従来の環境変数ログイン = admin」）|
| 22_スタッフアカウント仕様 | 全員同一権限、スタッフアカウント化が前提 |

**推奨**: 環境変数ログイン廃止時期を明記。完全移行後は staff_id の NOT NULL 制約追加。

---

## 🔵 軽微（表記ゆれ・参照エラー）

### 15. 変更履歴の実装状態記号が混在

99_変更履歴.md 内で `[x]`, `✅`, `[ ]` が混在。表記を統一すべき。

### 16. 05_LINEミニアプリ仕様書の最終更新日

最終更新が `2025-02-08` と記載（プロジェクトは2026年）。年の誤記の可能性。

### 17. RLSポリシーの設計方針が文書間で異なる

- スタッフ操作ログ: サービスロール経由で挿入
- ユーザイベントログ: ユーザー自身が挿入可能

意図的な分離であれば問題ないが、方針の理由を明記すべき。

### 18. stamp_count のデータ型表記ゆれ

| ドキュメント | 型 |
|-------------|-----|
| 05_Database_Schema | INTEGER（10点 = 1スタンプ） ✅ 確定 |
| 20_家族ひもづけ仕様検討 | NUMERIC（0.1単位）← 案4で解消済み |

20の旧案記載が残っているため、混乱の元になりうる。

---

## 対応優先度まとめ

| 優先度 | 件数 | 対応内容 |
|--------|------|---------|
| 🔴 即時対応 | 6件 | DB定義との矛盾修正、API仕様統一、スキーマ更新 |
| 🟡 短期対応 | 8件 | フェーズ定義統一、実装状態確認、保持期間定義 |
| 🔵 継続改善 | 4件 | 表記統一、年の誤記修正、方針の明記 |
