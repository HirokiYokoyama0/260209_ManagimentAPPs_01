# 実装状況の確認結果：正しい仕様

**確認日:** 2026-03-26
**確認方法:** 実際のSupabaseデータベースとコードを調査

---

## 🔍 調査結果サマリー

### 質問1: stamp_method の値は "qr" vs "qr_scan" どちらが正しいか？

**回答：両方とも正しいが、**現在の実装では **"qr"** が主流**

### 質問2: location パラメータは "premium/standard" vs "entrance" どちらが正しいか？

**回答： "entrance" が正しい**

---

## 📊 実際のデータベース調査結果

### stamp_method の使用状況

**実際のデータベースの内訳（最新1000件）：**

| stamp_method | 件数 | 使用状況 |
|--------------|------|---------|
| `"slot_game"` | 79件 | ✅ スロットゲーム |
| `"manual_admin"` | 22件 | ✅ スタッフ手動付与 |
| **`"qr_scan"`** | **16件** | ⚠️ **旧実装（古いAPI）** |
| **`"qr"`** | **5件** | ✅ **現在の実装** |
| `"survey_reward"` | 3件 | ✅ アンケート報酬 |

**重要な発見：**
1. `"qr_scan"` は16件（古いレコード、notesがnull）
2. `"qr"` は5件（新しいレコード、notesに"カメラ用QR (entrance)"が記録されている）

---

## 🔧 コードの実装状況

### 1. 現在使用されている stamp_method

| API | stamp_method | ファイル | 行番号 |
|-----|--------------|---------|-------|
| ✅ **QRスキャン（ペイロード型）** | `"qr"` | app/api/stamps/scan/route.ts | 155 |
| ✅ **QR自動付与（カメラ型）** | `"qr"` | app/api/stamps/auto/route.ts | 99 |
| ⚠️ **旧QRスキャンAPI** | `"qr_scan"` | app/api/stamps/route.ts | 123 |
| ✅ スタッフ手動付与 | `"manual_admin"` | app/api/stamps/manual/route.ts | 116 |
| ✅ アンケート報酬 | `"survey_reward"` | app/api/survey/submit/route.ts | 163 |
| ✅ スロットゲーム | `"slot_game"` | app/api/stamps/slot/route.ts | 84 |

**結論：**
- **現在の実装**では `"qr"` を使用（2つのAPI）
- **旧実装**では `"qr_scan"` を使用（1つのAPI、まだ残っている）

---

### 2. location パラメータの実装

**実際のデータベースのnotesフィールド（QRレコード）：**

| notes | 件数 | stamp_method |
|-------|------|--------------|
| `(null)` | 16件 | `"qr_scan"` ← 旧実装 |
| **`"カメラ用QR (entrance)"`** | **5件** | **`"qr"`** ← **現在の実装** |

**コードでの使用状況：**

**[app/api/stamps/auto/route.ts:103](app/api/stamps/auto/route.ts#L103)**
```typescript
notes: location ? `カメラ用QR (${location})` : "カメラ用QR",
```

**実際のURLパラメータ：**
```
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=10&location=entrance
```

**結論：**
- **location パラメータは `"entrance"` が実際に使用されている**
- `"premium"` や `"standard"` は使用されていない

---

## ❌ Doc 47 の誤り

### 誤り1: stamp_method の記載

**Doc 47（誤り）：**
```markdown
| stamp_method | 説明 |
|--------------|------|
| `"qr"` | QR来院スタンプ（Premium/Regular） | 既存 |
| `"purchase_incentive"` | 購買インセンティブ | **新規追加** |
```

**正しい記載：**
```markdown
| stamp_method | 説明 | 状況 |
|--------------|------|------|
| `"qr"` | QR来院スタンプ（Premium/Regular） | ✅ 現在の実装 |
| `"qr_scan"` | QR来院スタンプ（旧実装） | ⚠️ 後方互換性のため残存 |
| `"purchase_incentive"` | 購買インセンティブ | **新規追加** |
```

---

### 誤り2: location パラメータ

**Doc 47（誤り）：**
```
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=10&location=premium
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=5&location=standard
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=5&location=purchase
```

**正しいURL：**
```
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=15&location=entrance
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=10&location=entrance
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=5&location=shop
```

**または（type で区別する案）：**
```
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=15&location=entrance
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=10&location=entrance
https://liff.line.me/{LIFF_ID}?action=stamp&type=purchase&amount=5&location=shop
```

---

## 📋 型定義の確認

**[types/stamp.ts:13](types/stamp.ts#L13)**
```typescript
stamp_method: "qr" | "qr_scan" | "manual_admin" | "import" | "survey_reward" | "slot_game";
```

**これは正しい：** 両方とも含まれている（後方互換性のため）

---

## ✅ 正しい仕様まとめ

### 1. stamp_method

| 値 | 説明 | 使用API | 状況 |
|----|------|---------|------|
| `"qr"` | QR来院スタンプ | `/api/stamps/scan`, `/api/stamps/auto` | ✅ 現在の実装 |
| `"qr_scan"` | QR来院スタンプ（旧） | `/api/stamps` | ⚠️ 旧実装（後方互換性） |
| `"purchase_incentive"` | 購買インセンティブ | **新規追加予定** | 🆕 未実装 |
| `"manual_admin"` | スタッフ手動付与 | `/api/stamps/manual` | ✅ 実装済み |
| `"survey_reward"` | アンケート報酬 | `/api/survey/submit` | ✅ 実装済み |
| `"slot_game"` | スロットゲーム | `/api/stamps/slot` | ✅ 実装済み |
| `"import"` | データ移行 | - | ✅ 型定義のみ |

---

### 2. location パラメータ

**現在の実装：**
```
location=entrance  （来院QR用）
```

**新規追加予定：**
```
location=shop  （購買インセンティブ用）
```

**または type で区別：**
```
type=qr        （来院QR）
type=purchase  （購買インセンティブ）
```

---

## 🔄 Doc 47 の修正方針

### 修正箇所1: カメラ用QRコードのURL

**変更前（誤り）：**
```
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=15&location=premium
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=10&location=standard
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=5&location=purchase
```

**変更後（正）：**
```
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=15&location=entrance
https://liff.line.me/{LIFF_ID}?action=stamp&type=qr&amount=10&location=entrance
https://liff.line.me/{LIFF_ID}?action=stamp&type=purchase&amount=5&location=shop
```

---

### 修正箇所2: stamp_method の説明

**追加説明が必要：**

```markdown
### stamp_method について

現在のミニアプリでは、以下の2つの値が混在しています：

| stamp_method | 説明 | 使用状況 |
|--------------|------|---------|
| `"qr"` | QR来院スタンプ | ✅ 現在の実装（2024年3月以降） |
| `"qr_scan"` | QR来院スタンプ（旧） | ⚠️ 旧実装（後方互換性のため残存） |

**重要：** 新規にQRコードを生成する場合は、`stamp_method="qr"` が使用されます。
```

---

## 🎯 推奨実装方針

### 購買インセンティブのパラメータ設計

#### 案A：location で区別（推奨）

```
来院QR:
  type=qr, location=entrance

購買インセンティブ:
  type=qr, location=shop
```

**メリット：**
- 既存のコード構造に合致
- [app/api/stamps/auto/route.ts:103](app/api/stamps/auto/route.ts#L103) のロジックがそのまま使える

**実装箇所：**
```typescript
// app/api/stamps/auto/route.ts
stamp_method: location === "shop" ? "purchase_incentive" : "qr",
notes: location === "shop"
  ? "購買インセンティブ (カメラ用QR)"
  : `カメラ用QR (${location})`,
```

---

#### 案B：type で区別

```
来院QR:
  type=qr, location=entrance

購買インセンティブ:
  type=purchase, location=shop
```

**メリット：**
- 意味が明確
- URLを見ただけで区別できる

**デメリット：**
- [app/api/stamps/auto/route.ts:31](app/api/stamps/auto/route.ts#L31) のバリデーションを変更する必要がある

```typescript
// 変更前
if (type !== "qr") {
  return NextResponse.json(...);
}

// 変更後
if (type !== "qr" && type !== "purchase") {
  return NextResponse.json(...);
}
```

---

## 📝 まとめ

### ダッシュボード開発者への正しい情報

1. **stamp_method：**
   - 現在の実装では `"qr"` を使用
   - 購買インセンティブは `"purchase_incentive"` を新規追加

2. **location パラメータ：**
   - 来院QR: `location=entrance`
   - 購買インセンティブ: `location=shop`（推奨）

3. **URL形式：**
   ```
   Premium（15個）: ?action=stamp&type=qr&amount=15&location=entrance
   Regular（10個）: ?action=stamp&type=qr&amount=10&location=entrance
   Purchase（5個）:  ?action=stamp&type=qr&amount=5&location=shop
   ```

---

**作成者:** Claude Code
**確認方法:** 実データベース調査 + コード分析
**最終更新日:** 2026-03-26
